django>=4.2,<5.0
pillow>=10.0          # required by Photo.image (ImageField)
python-decouple>=3.8
djangorestframework>=3.14
django-cors-headers>=4.0
django-filter>=23.0   # DjangoFilterBackend for ViewSet filtering
